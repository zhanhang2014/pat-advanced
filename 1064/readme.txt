#题意

给定一个非负整数序列，其中序列总数N<1000,取值范围[0,2000],输出其对应的CBT遍历。

#思路

##输入

标准输入cin序列总数N，再for循环N次输入序列各值。

##算法

直接使用<algorithm>中的sort()函数对序列排序。
排序后利用CBT的下标特点，递归，中序遍历，下标存入<vector>。
按照得到的下标for循环生成新序列。

##输出

for循环标准输出cout新序列。